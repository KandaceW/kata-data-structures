module.exports = find;

function find(arr, searchDetails) {
  console.log(arr);
  console.log(searchDetails);
}
//object.keys
//object.values
//item = arr[i]
// for loop and if statement for a test
//finds the first matching object in the array
